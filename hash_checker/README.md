## Hash Checker

Generates SHA256 hashes for text and compares them to detect changes.

Demonstrates how hashing is used to verify file integrity — if a file changes, its hash completely changes, instantly revealing tampering.
