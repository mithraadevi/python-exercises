## Encryption/Decryption Tool

Encrypts text using XOR encryption with a secret key.

How it works: The same key that encrypts also decrypts. This is symmetric encryption. XOR is simple but demonstrates the concept — real systems use stronger algorithms like AES.
