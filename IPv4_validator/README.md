## IP Address Validator

Checks whether a given string is a valid IPv4 address. Validates format (4 segments) and range (0-255 per segment).

Demonstrates input validation — a fundamental networking and security concept.
