## File Integrity Checker

Generates SHA256 hashes for text and compares them to detect changes.

Use case: Verify downloaded files haven't been tampered with or corrupted by comparing hashes. Even a single character change produces a completely different hash — that's the power of hashing.
